# Environment for reproducing the results in this repository. The versions
# that carry the numbers are pinned; the rest float. Julia itself is
# bootstrapped by juliacall on first call, so nothing to install by hand.
#
# CI installs a different set inline: it skips numpyro, diffrax and optimistix
# because it runs no NUTS or Diffrax code, and it adds sphinx. That last one
# matters here: four tests need sphinx or docutils and skip without them, so
# this file gives 41 of 45 tests. Add sphinx to get all 45.
tesseract-core[runtime]==1.11.0
tesseract-jax==0.4.1
jax==0.11.0
juliacall==0.9.31
optax
equinox
numpy
scipy
sympy
matplotlib
numpyro
pytest
cma
# only for scripts/diffrax_event_gradient.py, which regenerates the one
# quantitative claim this project makes about another library
diffrax==0.7.1
optimistix==0.1.0
