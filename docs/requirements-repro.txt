# Pinned environment for reproducing the results in this repository.
# Julia itself is bootstrapped by juliacall on first call; nothing to install
# by hand. CI installs an equivalent set inline; it skips matplotlib and
# numpyro because it runs no figure or NUTS code.
tesseract-core[runtime]==1.11.0
tesseract-jax==0.4.1
jax==0.11.0
juliacall==0.9.31
optax
equinox
numpy
scipy
sympy
matplotlib
numpyro
pytest
cma
# only for scripts/diffrax_event_gradient.py, which regenerates the one
# quantitative claim this project makes about another library
diffrax==0.7.1
optimistix==0.1.0
